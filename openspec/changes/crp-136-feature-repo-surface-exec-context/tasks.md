## 1. Preflight Inventory

- [x] 1.1 Re-read `feature_repo_surface_and_exec_context.sh`, `feature_repo_surface_and_exec_context_rule.md`, and both `check_feature_repo_surface_and_exec_context_{be,fe}_quality.sh`; record the old-responsibility → new-owner inventory (context/gate/sync/skill; `--class` param; no capture, no readiness verb; class selection → orchestrator)
- [x] 1.2 Extract every old final-response line (track-parameterized success + infeasibility), gate exit-code rule, required-input set (definition + ears + contract_delta always; optional blueprint + in-flight plans), single-target allowed-write, per-class binding (be/fe assets, output filename, mobile→fe), ready-repo-or-blueprint scan-scope logic, and class-selection behavior into the parity checklist
- [x] 1.3 Diff the BE vs FE helper to extract the exact per-class config delta (title, section-4 heading, the seven `### 3.x` layer names, the eight `### 4.x` surface names, `project_classes` assertion); list the scenarios in `feature_repo_surface_and_exec_context_be_tests.sh` and `..._fe_tests.sh` to port

## 2. asdlc-coordinator core — validator, context, sync

- [x] 2.1 Implement `validate/surface-map.ts`: one shared awk-parity engine (placeholder detection incl. `[OPTIONAL…]`; title; four sections; nine filled meta keys with `project_type_code ∈ {A,B,C}` and `last_updated` `YYYY-MM-DD`; three scope keys; seven `### 3.x` layers each with `responsibility_summary`/`main_repo_paths`/`key_components`/`transport_layer`/`user_reachable_surface`; `### 3.8 Another Layer(s)`; eight `### 4.x` surfaces each with `surface_summary`/`applicability`/`repo_paths`/`why_feature_touches_it`/`expected_changes`/`evidence`/`transport_layer`/`user_reachable_surface`; ≥1 `applicability: applicable`) selected by a per-class config (backend vs frontend; mobile→frontend); exit `0/1/2` with the same `quality gate failed: …`/pass messages; export from `validate/index.ts`
- [x] 2.2 Implement `context/surface-map.ts` (takes class): resolve feature path + `projects/<id>` root (exit `2` if not under `projects/<id>/<feature>`); resolve read-only definition + ears + contract_delta (exit `2` if any absent); verify class ∈ active `meta_info.project_classes` (exit `2` otherwise); resolve scan scope = class ready repo (`collectReadyRepoPaths` filtered + `checkRepoBranchState`, block exit `2` verbatim) OR `project_stack_blueprint_<class>.md` fallback, exit `2` if neither; collect in-flight `implementation_plan.md` via reused `listCommittedSiblingFeatures`; emit per-class binding (track label, be/fe template+golden asset refs, target+allowed-write `project_surface_struct_resp_map_<class>.md`), scan-scope line, one `- read_only_input:` manifest entry per resolved input (3 always-on + optional blueprint + in-flight plans), `project_classes` value, exact `surface-map --class <class>` gate command; no repo writes; export from `context/index.ts`
- [x] 2.3 Implement `sync/surface-map.ts` (takes class): sync the class's ready repo via `collectReadyRepoPaths` filtered + `syncRepoToDefaultBranch`; blueprint-fallback class is a no-op exit `0`; unsyncable repo exit `2` with blocking message; export from `sync/index.ts`

## 3. asdlc-coordinator core — CLI wiring

- [x] 3.1 Add a `--class <backend|frontend|mobile>` option parser to `cli/run.ts` for `gate`/`context`/`sync surface-map` (mirroring the capture-options parser); validate the value and exit `2` with a usage error on missing/unknown class; pass the class through to the registered validator/builder/syncer
- [x] 3.2 Register `surface-map` in `gateRegistry`, `contextRegistry`, and `syncRegistry`; confirm unknown-step + missing-arg/usage errors behave with parity to the other verbs

## 4. asdlc-coordinator core — tests

- [x] 4.1 `gate surface-map` tests for BOTH classes: exit `0` complete BE and FE artifacts; exit `1` for each — empty, `[UNFILLED]`/`[OPTIONAL…]`, missing title/section, missing each meta key, invalid `project_type_code`/`last_updated`, missing scope key, missing each layer (+ `### 3.8`) and layer field, missing each surface and surface field, no applicable surface, wrong `project_classes` for the class; exit `2` runtime/missing target; assert `mobile` uses the FE config; assert actionable messages — porting both `*_tests.sh` suites
- [x] 4.2 `context surface-map` tests: per-class binding + scan scope + manifest + gate command for `--class backend`/`frontend`/`mobile`; ready-repo scope vs blueprint-fallback scope; class not in `project_classes` exits `2`; neither repo nor blueprint exits `2`; missing required input exits `2`; dirty/wrong-branch repo blocks (exit `2` verbatim); skill-relative asset paths
- [x] 4.3 `sync surface-map` tests: ready class repo synced; blueprint-fallback no-op exit `0`; unsyncable exit `2`
- [x] 4.4 CLI `--class` tests: each verb with valid `backend`/`frontend`/`mobile`; missing `--class` and unknown `--class infra` exit `2` with usage error
- [x] 4.5 Run `npm test --workspace packages/asdlc-coordinator` green

## 5. Skill package

- [x] 5.1 Create `packages/installer/_data/skills/overmind-surface-map/SKILL.md` with frontmatter, purpose, required invocation (with `--class`), exact `context`/`gate surface-map --class <class>` commands, single allowed-write (`project_surface_struct_resp_map_<class>.md`), gate exit-code handling, and BOTH literal track-parameterized final-response lines (success + infeasibility) — only here
- [x] 5.2 Inline `feature_repo_surface_and_exec_context_rule.md` into `SKILL.md` (per-class binding consumed from context, scan-scope/blueprint-fallback handling, evidence/no-invention rules, in-flight-plan read-only handling, read-only inputs + single-target boundary, runtime path bindings); do not keep a separate rule asset
- [x] 5.3 Copy all four assets into `assets/`: `project_surface_struct_resp_map_be_TEMPLATE.md`, `..._fe_TEMPLATE.md`, `..._be_GOLDEN_EXAMPLE.md`, `..._fe_GOLDEN_EXAMPLE.md` (preserve filenames); use skill-relative `assets/...` references
- [x] 5.4 Run the parity sweep table (old script+rule+both helpers vs SKILL.md+context+gate+sync); resolve every `missing` row — confirm both track lines live only in `SKILL.md`, the per-class binding + blueprint fallback are preserved, class selection is orchestrator-owned, and the five-class read-only / single-target boundary is preserved

## 6. Installer + setup staging

- [x] 6.1 Add `overmind-surface-map` to `PACKAGED_SKILLS` in `packages/installer/src/init.ts` (install to `.codex`/`.claude`, payload validation, install metadata); keep `.overmind/overmind.js` the single CLI
- [x] 6.2 Extend `packages/installer/test/init.test.ts` for the seventh skill (hardcoded enumeration, not imported): add `overmind-surface-map` to `ALL_SKILLS` and add its `SKILL_ASSET_CHECKS` entry (all four asset names) so install/asset/no-CLI assertions and the `result.skillPaths` `deepEqual` cover all seven skills; add atomic-failure cases for missing `SKILL.md` and missing `assets/`
- [x] 6.3 Add `overmind-surface-map` to skill-staging in `project_setup_first_init_machine.sh` (preflight checks for its folder/`SKILL.md`/`assets/`); remove staging of `feature_repo_surface_and_exec_context.sh` (command) and `feature_repo_surface_and_exec_context_rule.md` (rule); remove the flat `project_surface_struct_resp_map_{be,fe}_TEMPLATE.md` and `..._{be,fe}_GOLDEN_EXAMPLE.md` from the flat staging arrays (now skill-local); **KEEP both `check_feature_repo_surface_and_exec_context_{be,fe}_quality.sh` helpers staged** (required by un-migrated step 7.1 — D10); keep the shared `class_repo_paths.sh`/`sync_repo_to_default_branch.sh`/`list_committed_sibling_features.sh`/`check_cross_class_peer_trigger.sh` and downstream un-migrated assets staged; retain the `feature_repo_surface_and_exec_context` row in `.setup/models.md`; ensure update mode removes the stale migrated command/rule AND flat templates/golden examples (but not the retained helpers)
- [x] 6.4 Extend the setup shell suites for the seventh skill: assert `project_setup_asdlc_tests.sh` (fresh) and `project_setup_update_project_tests.sh` (update) stage `.codex`/`.claude` `overmind-surface-map/SKILL.md` + all four `assets/`, update mode repairs a missing/stale folder, the migrated command/rule and flat surface-map templates/golden examples are absent (update mode removes stale copies), and the two surface quality helpers + shared repo/cross-class/sibling libs remain staged
- [x] 6.5 Run `npm test --workspace packages/installer`, `bash tests/ai_scripts/project_setup_asdlc_tests.sh`, and `bash tests/ai_scripts/project_setup_update_project_tests.sh` green

## 7. Feature e2e wiring

- [x] 7.1 Add `build_surface_map_prompt <class>` + `run_surface_map_skill <class>` (thin launcher: runtime bindings + exact `context`/`gate surface-map --class <class>` commands only; no literal final lines, no gate handling); apply the migration-guide e2e safeguards: run `overmind sync surface-map --class <class>` before the session, run Codex from the ASDLC runtime root, preflight-check the installed `overmind-surface-map` skill + `.overmind/overmind.js`, capture the model exit code without leaking `set -e`
- [x] 7.2 Bind/load model config (§7.1–§7.3): add `SURFACE_MAP_MODEL_PHASE="feature_repo_surface_and_exec_context"` (reuse the existing `.setup/models.md` row) and `SURFACE_MAP_SKILL_FILE`; call `load_model_config` to populate `MODEL_CMD`/`MODEL_MODEL`/`MODEL_ARGS`, assert `MODEL_CMD=codex` (die otherwise), and build the Codex command from the loaded model/args — no hardcoding, no stale globals
- [x] 7.3 Read-only guard in `run_surface_map_skill`: derive the read-only set from the context `read_only_input` manifest (run `context surface-map --class <class>`, parse `- read_only_input:` lines; fail the phase if context exits non-zero), snapshot each before the session and `cmp -s`-assert each byte-unchanged after — evaluated on EVERY exit path, before returning the model rc; on read-only drift `die` with the corruption error even when the model also exited non-zero; also assert the per-class output `project_surface_struct_resp_map_<class>.md` was produced
- [x] 7.4 Rewire `run_phase7_loop` "Analyze one class now" to select a pending class and call `run_surface_map_skill <class>` (class selection owned by the loop; the skill never prompts for class); remove the `feature_repo_surface_and_exec_context.sh` entry from the phase-7 loop; keep completion tracking (map file exists → completed) so the loop advances class-by-class; add restart guidance resuming at phase 7
- [x] 7.5 Update `tests/ai_scripts/project_add_feature_e2e_tests.sh`: assert phase 7 prompts for a pending class, runs `overmind sync surface-map --class <class>` then launches the skill (stub `codex`), prompt has the exact `--class` commands and neither literal track line, the orchestrator does not run the gate, the launcher loads the `feature_repo_surface_and_exec_context` row and invokes the stub with its configured model/args, phase 7 fails when that row's command is not `codex`, phase 7 fails before launch when the skill/CLI is missing, phase 7 fails after the run when the stubbed model mutates any read-only input (incl. a blueprint and an in-flight plan) — including the model-also-exits-non-zero double-failure case (read-only-corruption error, not silent pass-through), phase 7 fails when the model exits 0 without producing the per-class output, a completed class advances the loop to the next pending class, and `mobile` writes `…_mobile.md` via the FE path; run green

## 8. Clean break + docs

- [x] 8.1 Delete `feature_repo_surface_and_exec_context.sh`, `feature_repo_surface_and_exec_context_rule.md`, `tests/ai_scripts/feature_repo_surface_and_exec_context_be_tests.sh`, `tests/ai_scripts/feature_repo_surface_and_exec_context_fe_tests.sh` (leave the two surface quality helpers — required by step 7.1 — and the shared repo/cross-class/sibling libs and their tests in place)
- [x] 8.2 Remove all references to the deleted files from setup staging arrays, shell test listings, the `CLAUDE.md` and `AGENTS.md` test lists, `README.md`/`QUICKRUN.md`
- [x] 8.3 Mark step 7 **done** in `design_docs/to_skills_migration/migration_to_skill_architecture_overview.md` Steps→Skills table

## 9. Verification

- [x] 9.1 Run `npm test --workspace packages/asdlc-coordinator`, `npm test --workspace packages/installer`, `bash tests/ai_scripts/project_setup_asdlc_tests.sh`, `bash tests/ai_scripts/project_setup_update_project_tests.sh`, `bash tests/ai_scripts/project_add_feature_e2e_tests.sh`
- [x] 9.2 `git diff --check` and `openspec validate crp-136-feature-repo-surface-exec-context --strict`
- [x] 9.3 Manual smoke: build, stage workspace, confirm `.codex`/`.claude` `overmind-surface-map/SKILL.md` + four assets + `.overmind/overmind.js`; run `sync`/`context`/`gate surface-map --class backend` and `--class frontend` against incomplete then complete fixtures (exit `1` then `0`); confirm blueprint-fallback context when no repo is ready; confirm the track-parameterized final line
