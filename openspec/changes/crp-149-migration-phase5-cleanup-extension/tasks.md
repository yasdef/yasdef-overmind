## 1. Extension Design Doc Revision (precedes dashboard implementation)

- [x] 1.1 Revise `design_docs/overmind_vscode_extention/requirements_ears.md` Requirement 7 clauses (run `.commands/*.sh`, script allow-list, "script missing or not executable" block) and Requirement 9 verification to the `overmind` verb allow-list, the `.overmind/overmind.js`/bundled-core availability check, and coordinator-primitive mutation paths per `03_target_architecture.md ### Supersession of the extension design docs`.
- [x] 1.2 Revise `technical_requirements.md` and `implementation_plan.md` Run Scanner / Create Feature / Continue E2E actions and the "script contract vs terminal mode" framework to `overmind status` / in-process `sequencing/`, `overmind scaffold feature`, and terminal-hosted `overmind run` per the same mapping. Remove scanner freshness/stale-state caveats and scanner-component language: manual refresh and file watching trigger fresh in-process `sequencing/` recomputation, with no terminal scanner action or persisted stale scanner state. Remove the `Create Project` terminal action from the revised extension plan and record it as postponed until a shared coordinator primitive or shipped `overmind` verb exists; this change SHALL NOT invent a replacement verb or keep launching `.commands/project_setup_add_new_project.sh`.
- [x] 1.3 Remove the supersession banners from all three extension docs and confirm no revised clause still names a deleted `.commands/*.sh` script as an extension action. This revision MUST land before Section 2 so the dashboard is built against the corrected plan, not the superseded launcher plan.

## 2. Extension Dashboard (after the doc revision in Section 1)

- [x] 2.1 Add failing coordinator tests for mandatory `asdlc-coordinator/workspace` and `asdlc-coordinator/sequencing` package subpaths, asserting `types`/`default` export-map targets, public API resolution, and transitive-import isolation from `cli/` and `orchestrator/`. Add failing `packages/vscode-extension` tests for the reused `sequencing/toFeatureSummary` projection, the total-over-declared-steps property, the diagnostic-carrying degraded path, the view/provider render path, imports exclusively from the two coordinator subpaths, and a static manifest/activation validation (asserts `publisher`, `engines.vscode`, `main`, the contributed dashboard view, `activationEvents`, and an exported `activate`).
- [x] 2.2 Add the mandatory subpath entries to `packages/asdlc-coordinator/package.json` with `types` and `default` targets, preserving the existing root `.` export, and make the coordinator isolation tests pass.
- [x] 2.3 Implement the read model in `packages/vscode-extension/src` as glue that calls `asdlc-coordinator/workspace` resolution and `asdlc-coordinator/sequencing` `ProgressReport` computation and obtains the `FeatureSummary` by reusing the existing `toFeatureSummary(report)` export — no root-barrel import, second `ProgressReport → FeatureSummary` mapping, independent step scan, definition parse, or filesystem walk.
- [x] 2.4 Make `packages/vscode-extension/package.json` a valid VS Code extension manifest (add `publisher`, `engines.vscode`, `contributes` for the read-only view, `activationEvents`, and `main` → an `activate`-exporting entrypoint; add `@types/vscode` as a devDependency only) and implement activation plus the contributed minimal read-only dashboard view/provider that renders the projected `FeatureSummary` and contributes no mutating command, form, or terminal; keep the render/provider path unit-testable in-process without launching a VS Code extension host.
- [x] 2.5 Surface `ProgressReport` `Diagnostic` values into a degraded-but-renderable result rather than throwing for data-level problems, matching the CLI's errors-as-values rendering, and render them in the view.
- [x] 2.6 If an additional resolution/read helper is needed, expose it only through the mandatory `workspace`/`sequencing` subpaths without new orchestrator coupling or changed `ProgressReport` semantics; keep `asdlc-coordinator` as the extension's sole runtime dependency.

## 3. Orphan Removal (Audit-Gated) + Deployed Cleanup

- [x] 3.1 Audit every `overmind/scripts/common_libs` helper for a functional production consumer — an active runtime/setup/update script that sources, executes as a child process, or otherwise invokes it (e.g. `feature_assing_workers.sh` executes `check_implementation_plan_readiness.sh`) — explicitly not counting its own `STAGED_COMMAND_LIB_FILES` entry or its own dedicated `tests/ai_scripts/*` suite as a consumer; record the surviving consumer for each retained helper.
- [x] 3.2 Delete the helpers with no functional consumer, their staging entries, and their dedicated shell tests; retain every still-consumed helper and its staging entry.
- [x] 3.3 Add an update-mode cleanup for removed command libs via an explicit `OBSOLETE_STAGED_COMMAND_LIB_FILES` tombstone list that `stage_command_libs()`/`stage_commands()` removes by name from a deployed workspace, covering every helper removed in 3.2. Do NOT blanket-sweep unmanaged files: the staged `common_libs` directory preserves operator content today and is not package-owned; a managed-manifest sweep is permitted only if this change first adds an explicit ownership contract for that directory.
- [x] 3.4 Add a direct-upgrade regression test that stages a removed helper into a fixture workspace, runs update mode, and asserts the stale staged helper is gone (this test may name the removed helper); assert an unmanaged operator-placed file in staged `common_libs` is preserved and that still-managed libs are never deleted.
- [x] 3.5 Retain the `OBSOLETE_STAGED_COMMAND_FILES` tombstones in `project_setup_first_init_machine.sh` — they still delete legacy staged scripts from workspaces upgrading directly from older releases; do not prune them absent a versioned migration proof. Retain new command-lib tombstone entries under the same retain-until-proven rule.
- [x] 3.6 Re-run the `CLAUDE.md`-named `tests/ai_scripts/*.sh` suites and confirm no active operator-facing surface references a deleted helper and the runtime stays green.

## 4. Versioned Doc Sweep

- [x] 4.1 Grep root `README.md` (there is no `overmind/README.md`; the plan's reference resolves here), `QUICKRUN.md`, and repository command references for `init_progress_scanner.sh`, `project_add_feature_e2e.sh`, `feature_br_scaffold.sh`, `persist_class_repo_attach.sh`, `project_contract_reconciliation.sh`, and any other Slice 1–4 deleted script, plus superseded workflow language (including `README.md`'s "separate legacy staged flow" and "until ... project reconcile lands in Slice 4" notes now that it shipped).
- [x] 4.2 Update those docs in place to describe only the shipped `overmind` verbs and surviving scripts; do not fork parallel doc variants. Scope the sweep to active operator-facing guidance only — leave `OBSOLETE_STAGED_COMMAND_FILES`/`OBSOLETE_STAGED_COMMAND_LIB_FILES` tombstones, cleanup regression tests, archived changes, and design/migration historical narratives untouched even though they still name deleted scripts.
- [x] 4.3 Update `AGENTS.md` in the same change if any canonical test command, path, or convention shifted, and assert no active operator-facing doc names a deleted flow.

## 5. Formal Parity Gate Closure

- [x] 5.1 Fold `05_parity_reconciliation.md ## Sweep result — behavior-parity gate CLOSED (Slice 3)` into `02_responsibility_translation_map.md ## Full parity gate`, marking the gate closed with the executed Slice 3 sweep as evidence.
- [x] 5.2 Retire the rows 18–20 "detection only" caveat, pointing those rows at their Slice 4 execution tests.
- [x] 5.3 Audit every row of `02_responsibility_translation_map.md` as owned-by-a-named-module or explicitly-retired, resolving any row that lacks a disposition ("any missing row blocks the migration").

## 6. Verification

- [x] 6.1 Run the extension read-model tests, the command-lib direct-upgrade cleanup test, plus the surviving `tests/ai_scripts/*.sh` suites; resolve every failure before broad verification.
- [x] 6.2 Run `npm run verify` (typecheck, type-checked lint, format check, build, all tests) green locally and confirm `packages/asdlc-coordinator` runtime `dependencies` remain empty.
- [x] 6.3 Run `openspec validate crp-149-migration-phase5-cleanup-extension --strict` and `git diff --check`.
