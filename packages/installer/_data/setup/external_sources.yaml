# External sources available to ASDLC phases.
# Each entry requires a matching MCP server configured in .claude/settings.json (or equivalent).
# The model uses 'type' to select the right source for each phase; 'name' must match the MCP server name exactly.
# Remove entries or leave sources empty if no external sources are configured.

sources: []

# Example:
# sources:
#   - name: tech-standards-kb
#     type: stack_knowledge_base
#     description: Company-approved tech stack standards and framework choices
#
#   - name: api-design-guide
#     type: api_standards
#     description: Internal API design guidelines and contract conventions
